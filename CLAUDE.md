# インタラクティブML学習サイト — プロジェクト決定事項

このファイルは計画フェーズ（承認済み 2026-07-03）の決定事項。すべてのサブエージェントは
作業前にこれを読むこと。技術詳細は `docs/tech-spec.md`、フェーズ成果物は `docs/` 配下を参照。

## プロジェクトの目的

大学院講義「Machine Learning for Innovation（#ESDD508、全7回、英語スライド）」の内容を、
**パラメータを操作しながら試行錯誤して学ぶインタラクティブ可視化教材サイト**（日本語）にする。
原典スライドは `reference/ML4Innovation00–07.pdf`。

学習者プロファイル（最重要。すべての文章はこの読者に向けて書く）:
- 機械工学専攻の修士学生。**情報工学はほぼ未習**。線形代数・微積分は既習
- 就活・研究で多忙 → 1モジュール約90分×7回で完結させる
- 目的: ①授業の理解・単位対策 ②就活・キャリアの教養 ③研究へのML活用
- 将来外部公開の可能性あり → 初見の第三者が読んでも理解できる説明的な文章にする

## 文章・説明のスタイル規約

- 日本語本文。専門用語は初出時に **日本語（English）** の対訳形式（授業スライドが英語のため対訳が単位対策に効く）
- 数式は「直感重視＋主要な式のみ」。導出の詳細は追わない。式の前に必ず言葉とイメージで説明する
- 機械工学の例えを一貫使用: 勾配降下＝ボールが谷を転がる／学習率＝一歩の幅／
  BatchNorm＝センサ信号の正規化／過学習＝実験ノイズまで再現する過剰な曲線あてはめ、など
- 各概念は「なぜ必要か（動機）→ 直感 → 式 → 可視化で試す」の順で提示
- 敬体（です・ます）で説明的に。箇条書きの乱用より流れのある文章を優先

## モジュール構成（7回×約90分）

各モジュール = 説明テキスト＋インタラクティブ可視化2〜3個＋確認クイズ5問前後＋教養コラム1本
＋（該当回は）Colabリンク。時間配分目安: 読解50分／可視化で試行錯誤30分／クイズ10分。

### M1: AI・機械学習・深層学習とは（原典: 00, 01）
- AI⊃ML⊃DLの関係、モデル＝パラメータを持つ関数 y=f(x;θ)、DLの4要素（データ・アーキテクチャ・損失・最適化）
- モデル容量と過学習/未学習、DL成功の3要因（モデル容量・ビッグデータ・計算技術）
- CVタスクの種類（分類・検出・セグメンテーション）、応用例（DeepL, ChatGPT, 画像生成）
- 可視化: **多項式フィッティング実験室**（次数・データ数・ノイズのスライダー → 過学習を体感）
- コラム: 生成AIまでの年表（就活教養）

### M2: ニューロンで論理を組む（原典: 02）
- ニューロン＝線形重み付け＋活性化 y=f(wᵀx+b)、シグモイド、sign
- 単層ネットでAND/OR/NOT、2層ネット＋one-hotでmin/max（授業課題1と同形式）
- 画像分類の標準データセット（MNIST, CIFAR）、Train/Validation/Testの使い分け（テストを学習に使う＝cheating）
- 可視化: **重み・バイアス操作盤**（スライダーで真理値表がPASS/FAILに変わる）／単一ニューロンの決定境界
- Colab: 論理演算・min/max練習ノートブック

### M3: CNNの構成要素（原典: 03）
- 活性化関数ファミリー（Sigmoid, tanh, ReLU, LeakyReLU）、one-hot、softmax、cross-entropy（KLとの関係は直感レベル）
- 畳み込み＝位置をずらしながら同じ重みを使い回す線形演算（パラメータ数の比較）、padding/stride、pooling
- CNN内のテンソル形状の流れ（3×32×32 → … → 100）
- 可視化: **畳み込みフィルタ実験室**（カーネル値編集→画像に即時適用）／活性化関数ギャラリー／softmax棒グラフ操作
- コラム: LLM APIに触れてみる（原典03のQuiz題材）

### M4: 学習のしくみ — 勾配降下と最適化（原典: 04前半）
- 損失関数、勾配降下 θ←θ−η∂L/∂θ、誤差逆伝播は「連鎖律で勾配をバケツリレー」の直感まで
- SGD・ミニバッチ・エポック、Momentum、Adam（式は結果のみ）、学習率スケジューリング（Step, Cosine）
- 可視化: **損失地形ビジュアライザ**（2D損失面をボールが降下、η・momentum・optimizer切替、発散も体験）／
  **ミニNN訓練場**（TensorFlow Playground風・ブラウザ内で2D分類を学習）
- Colab: CIFAR-100改善実験 その1（授業課題2に直結）

### M5: 過学習との戦い — データ拡張と正則化（原典: 04後半, 05）
- 過学習/未学習と汎化、学習曲線の読み方、データ拡張（flip/crop/色変換/RandomErase）とTest Time Augmentation
- アンサンブル → Dropout（弱いネットの多数決という直感）、BatchNorm（分布を揃えてから学習可能なγ,βで戻す）
- 可視化: **学習曲線シミュレータ**（データ量・Dropout率・拡張ON/OFF → train/val曲線の乖離）／
  **データ拡張プレビュー**／BatchNorm分布正規化アニメーション
- Colab: CIFAR-100改善実験 その2（Dropout/BN/DAを効かせる）

### M6: 転移学習 — 少ないデータで賢く（原典: 06）
- 小データで深層モデルを自前学習すると過学習 → 事前学習済み特徴抽出器＋古典分類器という実務パターン
- ImageNet、転移学習、img2feat＋scikit-learnパイプライン、自前データセットの作り方
- 可視化: **特徴空間エクスプローラ**（事前学習特徴の2D埋め込み散布図でクラス分離を観察）／転移学習パイプラインのステップ実行図解
- Colab: 自前画像の多クラス分類（授業課題3・最終レポート 2026-07-30締切に直結）
- コラム: 製造業のML活用事例（外観検査・サロゲートモデル・実験データ解析）

### M7: 物体検出と、その先へ（原典: 07）
- ロジスティック回帰（シグモイド＋交差エントロピー＋L2正則化λ）、ten-crop augmentation
- 物体検出: R-CNN系 vs YOLO、IoU（式で定義）、NMS
- 可視化: **IoU/NMSプレイグラウンド**（ボックスをドラッグ→IoU値が変化、NMSステップ実行）／
  ロジスティック回帰の決定境界＋正則化λスライダー
- コラム: 就活で聞かれるAIの話題／「次に学ぶなら」リンク集（強化学習・Transformer詳細は本教材のスコープ外）

## 技術方針（詳細は docs/tech-spec.md）

- Astro（静的サイト）+ MDX + React islands + TypeScript。数式はKaTeX
- 可視化はSVG/Canvas直描画＋軽量ユーティリティのみ（重いチャートライブラリは使わない）。
  ミニNN訓練は依存なしのTS実装
- クイズ進捗はlocalStorage（サーバー不要）
- デプロイ: 静的ビルド成果物をユーザー所有サーバーへ（rsync等）
- Colabノートブックは `notebooks/` で管理し、GitHub経由の「Open in Colab」バッジでリンク

## 進め方・フェーズ規約

1. 計画フェーズ（完了）: 本ファイルが成果物
2. 技術仕様フェーズ: `docs/tech-spec.md` を作成し、レビューエージェントの承認を得る
3. 実装フェーズ: コーディングはOpus/Sonnetサブエージェントに委譲。モジュール単位で分担
4. 各フェーズの成果物はMarkdownで `docs/` に残し、次のエージェントに引き継ぐ

## サブエージェント向け注意

- 文章を書くときは必ず上記「文章・説明のスタイル規約」に従うこと
- 原典PDFの記述と矛盾する内容を書かない（用語は原典スライドの英語表記を対訳で併記）
- インタラクティブ部品は「スライダー等の操作 → 結果が即時変わる」を最優先。初期値は
  意味のある状態にし、リセットボタンを付ける
- 検証: `npm run build` が通ることを確認してから完了報告すること
